# Implementation Plan

The canonical, up-to-date implementation plan lives in [docs/implementation_plan.md](docs/implementation_plan.md).

For the current state of every layer and module, see [docs/PROJECT_STATE.md](docs/PROJECT_STATE.md).

> This file used to contain its own copy of the plan; that copy went stale relative to the version in `docs/`. To prevent drift, the source of truth has been consolidated under `docs/` and this file is now a pointer.
